# 阶段复盘 · M{N} · {阶段简称}

> 每个 USER-TEST 通过后必产出一份。文件名：`phase-{N}-{slug}.md`。
> 写完后 `git tag m{N}-pass`。

---

## 1. 做了什么

> 交付清单。每行一个：[任务编号] - [文件 / 模块] - [一句话]。

| 任务 | 文件 / 模块 | 一句话说明 |
|---|---|---|
| T? | path/to/file | 做了什么 |

---

## 2. 如何测试

### 前置

- [ ] 必填环境变量已配置（参考 `.env.example`）
- [ ] 依赖已安装（`pip install -e .[dev]` / `npm ci`）
- [ ] 其他前置...

### 用例清单

| # | 用例 | 操作 | 预期结果 |
|---|---|---|---|
| 1 | xxx | xxx | xxx |

---

## 3. 测试结果

### 实测

| # | 用例 | 实测 | 状态 |
|---|---|---|---|
| 1 | xxx | xxx | ✅ / ⚠️ / ❌ |

### 失败 / 降级项

> 列出未达预期但仍被允许通过的项目，含理由与影响评估。

### 性能数据（如有）

| 指标 | 目标 | 实测 |
|---|---|---|
| 首屏 | ≤ 2s | xxx |
| LLM 首 token | ≤ 5s | xxx |

---

## 4. 如何复现

### 从零步骤

```bash
# 步骤 1
git checkout {commit_hash}

# 步骤 2
cp .env.example .env  # 填 key

# 步骤 3
docker compose up
```

### 耗时

| 步骤 | 时长 |
|---|---|
| 安装依赖 | xxx 分 |
| 测试 | xxx 分 |

### 已知坑

- xxx
